<p id="notice"><%= notice %></p>

<h1>Organizations</h1>

<table>
  <thead>
  <tr>
  </tr>
  </thead>
  <tbody>
  <% @organizations.each do |org| %>
    <tr>
      <td><%= link_to org.name , organization_path(org.id) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<br>
