<p id="notice"><%= notice %></p>
<div class="row ml-0 mt-3">
  <h2>Events</h2>
</div>
<div class="row ml-0 mt-2">
</div>
<div class="row ml-0 mt-2 mb-3">
  <%= link_to "Add Event", new_event_path, class: ["btn", "btn-primary"] %>
</div>
<div class="row">
  <% @events.each do |id| %>
    <%= render partial: 'event_tile', locals: { id: id} %>
  <% end %>
</div>
